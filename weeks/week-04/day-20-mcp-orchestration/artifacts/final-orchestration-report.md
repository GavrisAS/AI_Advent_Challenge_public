# Week 04 MCP orchestration report

## Tracker

- Issues: `5`
- Status counts: `{'done': 4, 'planned': 1}`

- `AI-16` — Подключить MCP discovery (`done`). Подключить публичный DeepWiki MCP server, выполнить initialization и получить список tools без вызова инструментов.
- `AI-17` — Подключить первый MCP tool (`done`). Реализовать локальный stdio MCP server поверх mock Tracker API и вызвать его из агента.
- `AI-18` — Расширить MCP orchestration (`done`). Реализовать scheduler-aware MCP tools с SQLite persistence, run log и aggregated Tracker summary.
- `AI-19` — Реализовать композицию MCP tools (`done`). Собрать LLM-driven pipeline search, deterministic report и safe file save внутри одного stdio MCP server.
- `AI-20` — Оркестрировать несколько MCP servers (`planned`). Добавить discovery, normalized registry, neutral JSON planner и routing между четырьмя локальными stdio MCP servers.

## Knowledge context

- `day-18` — Scheduler-aware MCP tools: Bounded periodic collection with SQLite snapshots and deterministic tools.
- `day-19` — Composition tools внутри одного MCP server: Planner выбирает search, report и save tools одного stdio server.
- `day-20` — Orchestration нескольких MCP servers: Host маршрутизирует neutral JSON actions между несколькими MCP sessions.
- `model-agnostic-planning` — Model-agnostic planning: Planner emits ordinary JSON actions instead of provider-native tool calls.
- `multi-server-orchestration` — Multi-server orchestration: Discovery, validation and routing must be explicit and auditable.

## MCP server topology

- Registered servers: `knowledge`, `report`, `storage`, `tracker`.
- Data sources used before report generation: `tracker`, `knowledge`.
- Current flow stage: `report_generation`.
- Tool calls completed before report generation: `7`.
- Model-specific tool calling: `false`
- Hardcoded pipeline: `false`

This report is generated by the `report` MCP server. After report generation and next-step
processing, it is persisted through the `storage` MCP server as part of the complete
orchestration flow. The final trace, not this pre-storage snapshot, proves which servers were
actually invoked through completion.

## Итог

Данные собраны с независимых Tracker и Knowledge MCP servers, обработаны Report server и должны
быть сохранены через Storage server. Planner использует переносимый JSON action contract.
